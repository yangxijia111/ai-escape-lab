/**
 * Benchmark identity. Bump when rooms, scoring, observation boundaries,
 * or any evaluation semantics change — runs are only comparable within
 * the same benchmarkVersion.
 */
export const BENCHMARK_VERSION = "1.1.0";
